# Copy to .env:  cp .env.example .env

# Local SQLite — path is relative to no-escape-back/
DATABASE_URL="file:./prisma/dev.db"

# Must match the UI (NoEscape-UI/src/app/core/api.config.ts)
PORT=3000
